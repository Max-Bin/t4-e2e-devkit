"""Simulation utilities for T4 planning evaluation.

Submodules are intentionally not imported here.  The trajectory package is
used by the common dataclasses, so eager closed-loop imports would create a
cycle during package initialization.
"""
